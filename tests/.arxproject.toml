[project]
name = "sciarx-tests"
version = "0.1.0"
edition = "2026"
authors = [
  { name = "Ivan Ogasawara", email = "[ivan.ogasawara@gmail.com]" }
]

[build]
src_dir = "./"
entry = "main.x"
out_dir = "build"

[arxpm.dependencies-dev]
dependencies = [
  "makim",
  "pre-commit",
]

[toolchain]
compiler = "arx"
linker = "clang"
