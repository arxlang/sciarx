import sciarx
import core from sciarx.core
import version from sciarx.version

# Minimal placeholder test file.
# For now this just ensures the public modules can be imported.

let imported_root_ok: bool = true
let imported_core_ok: bool = true
let imported_version_ok: bool = true
