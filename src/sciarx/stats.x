```
title: SciArx Library
```

fn sum2(a: float64, b: float64) -> float64:
    ```
    title: Sum of two numbers
    ```
    return a + b

fn mean2(a: float64, b: float64) -> float64:
    ```
    title: Mean of two numbers
    ```
    return (a + b) / 2.0
